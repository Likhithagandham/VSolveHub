import { AboutScreen } from '@/components/about/AboutScreen';

export const metadata = { title: 'About — VSolveHub' };

export default function AboutPage() {
  return (
    <div className="page-about">
      <AboutScreen />
    </div>
  );
}
