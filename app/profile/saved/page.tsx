import { ProfileSaved } from '@/components/profile/ProfileSaved';

export const metadata = { title: 'Saved Services — VSolveHub' };

export default function ProfileSavedPage() {
  return (
    <div className="page-profile">
      <ProfileSaved />
    </div>
  );
}
