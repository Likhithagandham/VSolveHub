import { ProfileMembership } from '@/components/profile/ProfileMembership';

export const metadata = { title: 'Plus Membership — VSolveHub' };

export default function ProfileMembershipPage() {
  return (
    <div className="page-profile">
      <ProfileMembership />
    </div>
  );
}
