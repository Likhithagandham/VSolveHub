import { ProfileScreen } from '@/components/profile/ProfileScreen';

export const metadata = { title: 'Profile — VSolveHub' };

export default function ProfilePage() {
  return (
    <div className="page-profile">
      <ProfileScreen />
    </div>
  );
}
