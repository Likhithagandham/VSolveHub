import { ProfileWallet } from '@/components/profile/ProfileWallet';

export const metadata = { title: 'Wallet — VSolveHub' };

export default function ProfileWalletPage() {
  return (
    <div className="page-profile">
      <ProfileWallet />
    </div>
  );
}
