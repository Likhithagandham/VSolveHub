import { ProfileAddresses } from '@/components/profile/ProfileAddresses';

export const metadata = { title: 'Addresses — VSolveHub' };

export default function ProfileAddressesPage() {
  return (
    <div className="page-profile">
      <ProfileAddresses />
    </div>
  );
}
